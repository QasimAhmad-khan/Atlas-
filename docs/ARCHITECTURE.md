# Architecture

This document will be completed as the implementation progresses through the
checkpoint plan. The initial design keeps API, crawling, parsing, pipeline,
persistence, and observability concerns in separate packages.
